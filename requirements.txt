This project has been ported to TypeScript. Use Node.js (>=18) and npm to install dependencies.

Install:

```bash
npm install
```

Development:

```bash
npm run dev
```

The Python `requirements.txt` is no longer used for the TypeScript port.